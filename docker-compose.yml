version: '3.7'

services:
  tomcat:
    image: tomcat:jre8-openjdk-bullseye
    volumes:
      - ./dist/sid_tp-api.war:/usr/local/tomcat/webapps/api.war
      - ./tomcat/import-data:/usr/local/tomcat/import-data
      - ./tomcat/logs:/usr/local/tomcat/logs
    ports:
      - 8080:8080
    networks:
      - tp_backend

networks:
  tp_backend:
    external: true
